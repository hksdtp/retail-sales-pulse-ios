import React from 'react';

interface StepIndicatorProps {
  currentStep: number;
}

const StepIndicator: React.FC<StepIndicatorProps> = ({ currentStep }) => {
  // Ẩn hoàn toàn step indicator theo thiết kế mới
  return null;
};

export default StepIndicator;
